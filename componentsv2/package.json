{
  "name": "components-v2-builder",
  "version": "1.0.0",
  "description": "Framework-agnostic constructor for Discord Components V2 (Display Components): types, builders, validators, serialization and Discord API import/export.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/"
  },
  "keywords": ["discord", "components-v2", "display-components", "builder"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
